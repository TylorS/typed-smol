{
  "name": "typed-smol",
  "private": true,
  "scripts": {
    "build": "tsc -b && pnpm --filter @typed/virtual-modules-vscode build",
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@10.0.0"
}
