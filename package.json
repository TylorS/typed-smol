{
  "name": "typed-smol",
  "private": true,
  "scripts": {
    "build": "tsc -b && pnpm -r build",
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@10.0.0"
}
