## Reflections

- Process improvements are more reliable when encoded in:
  1. canonical stage policy,
  2. canonical template,
  3. immediate in-flight document validation.
- Visual architecture requirements are high leverage for complex specs because they reduce ambiguity during review.
