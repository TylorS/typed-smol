# Workflow Init: TypeInfoApi structural type checking

- **objective**: Ensure virtual module plugins use strict structural type checking via TypeInfoApi; eliminate string-based type checks. If the API cannot support this, improve it.
- **started_at**: 2025-02-22
- **started_by**: agent (self-improvement-loop)
- **source_context**: User directive via self-improvement-loop: "we need to always write virtual module plugins utilizing strict structural type checking via the TypeInfoApi. If we can't do it yet, we need to improve that API. We MUST NOT do checking via string checks"
- **explicit_reuse_override**: false
