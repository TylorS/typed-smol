# Memory Episodes

- T1 created the host-neutral protocol package shell and id utilities with no Chrome/runtime imports.
- T2 added host-neutral schemas, HMR status facts, and bounded serialized values without importing runtime, compiler, or Chrome packages.
- T3 added the protocol-owned unstable RPC group and host-neutral fixtures without adding Chrome/runtime imports.
