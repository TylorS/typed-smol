# Workflow Init

- **objective**: Enable `typed serve` to run as a pure Vite SPA without a server entry; add configurable index.html location; create `typed:config` virtual module; expose Vite dev server as a virtual module for NodeHttpServer integration; create SSR wrapper integrating `transformIndexHtml`.
- **started_at**: 2026-02-26T14:00:00Z
- **started_by**: user
- **source_context**: packages/cli, packages/vite-plugin, packages/app, packages/ui, examples/counter
- **explicit_reuse_override**: false
- **mode**: strict
- **finalization_strategy**: merge
