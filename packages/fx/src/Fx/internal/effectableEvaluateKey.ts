/**
 * Must stay aligned with `evaluate` in `effect`’s `internal/core` (same string as
 * `` `${EffectTypeId}/evaluate` ``).
 *
 * @internal
 */
export const EFFECT_EVALUATE_KEY = "~effect/Effect/evaluate" as const;
