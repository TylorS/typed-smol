# @typed/tsconfig

## Intent

Shared TypeScript configs. Config-only package; no source code.

## Key exports / surfaces

- `./base`, `./dom`, `./test`, `./webworker` — JSON config files
- Optional dependency: `@typed/virtual-modules-ts-plugin`

## Constraints

- Monorepo governance: `.cursor/rules/monorepo-governance.mdc`

## Pointers

- Root AGENTS.md for bootup/modes
