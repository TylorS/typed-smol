{
  "name": "@typed/virtual-modules-ts-plugin",
  "version": "1.0.0-beta.4",
  "files": [
    "dist",
    "src"
  ],
  "type": "commonjs",
  "main": "dist/plugin.js",
  "exports": {
    ".": {
      "types": "./dist/plugin.d.ts",
      "require": "./dist/plugin.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup && cp src/plugin.d.ts dist/plugin.d.ts",
    "build:plugins": "pnpm --filter virtual-modules-ts-plugin-sample run build:plugins",
    "pretest": "pnpm --filter @typed/virtual-modules run build && pnpm --filter @typed/compiler run build && pnpm run build && pnpm --filter @typed/virtual-modules-compiler run build",
    "test": "pnpm run test:typecheck && vitest run --passWithNoTests",
    "test:typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@typed/compiler": "workspace:*",
    "@typed/virtual-modules": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "tsup": "^8.5.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "optionalDependencies": {
    "@typed/app": "workspace:*"
  }
}
